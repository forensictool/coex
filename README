Folder 'project' has project for qt.
	for build:
	cd project/computer_examination
	qmake && make

Folder 'docs' has documentation.

You can find repo with test data:

for example:
$ git clone git@git.keva.su:gpo-testdata.git gpo-testdata.git







