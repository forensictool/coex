Folder 'sources' has all sources.

Folder 'docs' has only documentation.

Folder 'examples' has some examples.

Folder 'thirdparty' has some "alien" packages.

File 'clean.sh' - script for clean temporary files

File 'build.sh' - script for build coex (without clean temporary files)

File 'rebuild.sh' - script for re-build coex (clean + build)

File 'test.sh' - script for test-run (use tmp/) after build

File 'clone-test-data.sh' - script for clone repository test-data to tmp/

File 'README' - this file

steps for test runs:
1. call script ./clone-test-data.sh
2. call script ./test.sh

