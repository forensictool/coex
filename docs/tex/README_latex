debian (650 mb):
# apt-get install texlive-full

$ pdflatex example.tex


and for convert from ODT to LaTex, see also: 
# apt-get install writer2latex

for correct compilation
$ pdflatex example.tex
$ bibtexu example.tex
$ pdflatex example.tex